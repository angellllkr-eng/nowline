{
  "name": "@nowline/services",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "node --check auth-gateway/index.js && node --check launcher/index.js && node --check mcp/webhook.js && node --check payments/webhook.js && node --check orchestrator/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jose": "^6.1.0",
    "stripe": "^18.5.0"
  }
}
